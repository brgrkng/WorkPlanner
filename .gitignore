# Local planning & tracking docs — deliberately not committed
PLANNER_HANDOFF_BRIEF.md
CLAUDE.md
SESSION.md
# Setup guide, kept local for consistency with the above. It contains no
# secrets, so committing it is safe if you'd rather have it in the repo.
FIREBASE_SETUP.md

# Firebase config — injected at build time from GitHub repository secrets.
# Never commit these; see PLANNER_HANDOFF_BRIEF.md §9.
.env
.env.*
!.env.example
.firebase/
firebase-debug.log*
firestore-debug.log*

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
dist-ssr/
build/

# Test & tooling caches
coverage/
.vite/
.vitest/
*.tsbuildinfo
.eslintcache

# Logs
*.log
npm-debug.log*
yarn-error.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Claude Code local overrides
.claude/settings.local.json
